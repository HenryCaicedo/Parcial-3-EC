El video a convertir debe llamarse 'video.mp4' y estar en la misma carpeta que el script

Correr estos comandos en la terminal:

# main opencv
pip install opencv-python
# contrib package for the extra features
pip install opencv-contrib-python
pip install moviepy
pip install ffmpeg
pip install ffmpeg-python
pip install pytube imageio imageio-ffmpeg

